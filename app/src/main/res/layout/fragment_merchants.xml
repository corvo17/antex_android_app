<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.merchants.MerchantsFragment">

    <!-- TODO: Update blank fragment layout -->
    <android.support.v7.widget.RecyclerView
        android:id="@+id/id_merchents_recycler_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scrollbars="vertical"
        android:scrollbarSize="20dp"
        android:scrollbarStyle="outsideOverlay"
        android:layout_above="@+id/id_tv_merchant_size"
        >


    </android.support.v7.widget.RecyclerView>

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/id_add_merchant_fab"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_add"
        android:layout_alignParentRight="true"
        android:layout_above="@+id/id_tv_merchant_size"
        android:layout_marginRight="16dp"
        android:layout_marginBottom="16dp"
        android:visibility="gone"
        />

    <android.support.v7.widget.AppCompatImageButton
        android:id="@+id/id_merchents_go_top"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:layout_above="@+id/id_tv_merchant_size"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="16dp"
        android:background="@drawable/round_grey_image"
        android:scaleType="centerCrop"
        android:src="@drawable/go_to_top" />

    <TextView
        android:id="@+id/id_tv_merchant_size"
        android:text="Merchants: 0"
        android:background="@color/colorPrimary"
        android:padding="8dp"
        android:textSize="18sp"
        android:fontFamily="@font/roboto_medium"
        android:textColor="@color/white"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        />


</RelativeLayout>